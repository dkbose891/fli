# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# env (config only, but never commit — see .env.example)
.env
.env.local
.env*.local

# macOS
.DS_Store

# misc
*.pem
npm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# local tooling + internal build spec (not for the public repo)
.claude/
instructions/
docs/superpowers/
